## 1.04-test-smartcontract-with-jest

1. Open 1.04-test-smartcontract-with-jest in VSCode
$ source ~/.bash_profile
nvm use v16.13.1
node -v

v16.13.1

2. Run truffle compile
$ truffle compile

3. Run yarn to install dependency from package.json
$ yarn

4. Run typechain to gen typescript version of contract
yarn run typechain --target=web3-v1 'build/contracts/*.json'

5. Run test
yarn run jest --testTimeout=60000

6. Fix the test case to fail
yarn run jest --testTimeout=60000

7. Explain test cases

8. Explain typechain