## 5.01-install-nuxtjs

1. Open 5.01-install-nuxtjs in VSCode
$ source ~/.bash_profile
nvm use v16.13.1
node -v

v16.13.1

2. Run yarn to install dependency
$ yarn

3. Run truffle compile
$ truffle complie

4. Run typechain to gen typescript version of contract
$ yarn run typechain --target=web3-v1 'build/contracts/*.json'

5. Run yarn dev
$ yarn dev

6. Explain sourcecode in pages/index.vue