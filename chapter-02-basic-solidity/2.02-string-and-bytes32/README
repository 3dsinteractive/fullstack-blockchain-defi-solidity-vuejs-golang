## 2.02-string-and-bytes32

1. Open 2.02-string-and-bytes32 in VSCode
Type the following command into console to use node version 16.13.1
$ source ~/.bash_profile
nvm use v16.13.1
node -v

v16.13.1

2. Run truffle compile
$ truffle compile

3. Run yarn to install dependency
$ yarn

4. Run typechain to gen typescript version of contract
yarn run typechain --target=web3-v1 'build/contracts/*.json'

5. Run test
yarn run jest --testTimeout=60000

6. Explain test cases

7. Check on different gas fee