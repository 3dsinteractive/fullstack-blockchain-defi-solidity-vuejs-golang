## 3.03-faucets

1. Open 3.03-faucets in VSCode
Type the following command into console to use node version 16.13.1
$ source ~/.bash_profile
nvm use v16.13.1
node -v

v16.13.1

2. Run yarn to install dependency, 
   We need to run yarn before truffle compile because we want @openzeppellin contracts
$ yarn

3. Run truffle compile
$ truffle compile

4. Run typechain to gen typescript version of contract
yarn run typechain --target=web3-v1 'build/contracts/*.json'

5. Run test
yarn run jest --testTimeout=60000

6. Fix the test case to fail
yarn run jest --testTimeout=60000

7. Explain test cases